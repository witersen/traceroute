
import Vue from 'vue'
import Vuex from 'vuex'
import Routers from '../router';
Vue.use(Vuex)

const state = {
    userInfo: ""
};

export default new Vuex.Store({
    getters: {
    },
    mutations: {
    }
})